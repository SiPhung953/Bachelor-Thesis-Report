%%{init: {
  "theme": "default",
  "htmlLabels": false,
  "themeVariables": {
    "fontFamily": "Times New Roman, serif",
    "fontSize": "18px"
  },
  "class": {
    "diagramPaddingX": 15,
    "diagramPaddingY": 15,
    "nodeSpacing": 40,
    "rankSpacing": 50,
    "hideEmptyMembersBox": true
  }
}}%%

classDiagram
direction LR

  %% ==========================================
  %% 1. PRESENTATION LAYER (Controllers)
  %% ==========================================
  class AuthController {
    <<Controller>>
    +register()
    +login()
    +logout()
    +requestPasswordReset()
    +resetPassword()
    +validateResetToken()
  }

  %% ==========================================
  %% 2. APPLICATION LAYER (Services)
  %% ==========================================
  class AuthService {
    <<Service>>
    +register()
    +login()
    +logout()
    +requestPasswordReset()
    +resetPassword()
    +validateResetToken()
  }

  %% ==========================================
  %% 3. INFRASTRUCTURE & UTILITY LAYER
  %% ==========================================
  class PasswordHasher {
    <<Utility>>
    +hash()
    +compare()
  }

  class ResetTokenUtils {
    <<Utility>>
    +generateToken()
    +hashToken()
  }

  class JwtService {
    <<Utility>>
    +sign()
    +verify()
    +decode()
  }

  class EmailService {
    <<Utility>>
    +sendPasswordResetEmail()
  }

  class AuditLogger {
    <<Utility>>
    +log()
  }

  class PrismaClient {
    <<Infrastructure>>
    +user
    +passwordResetToken
    +role
  }

  %% ==========================================
  %% RELATIONSHIPS (Flows Left-To-Right)
  %% ==========================================

  %% Presentation to Application
  AuthController --> AuthService : delegates to

  %% Service Dependencies
  AuthService --> PasswordHasher : uses
  AuthService --> ResetTokenUtils : uses
  AuthService --> JwtService : signs tokens
  AuthService --> EmailService : uses
  AuthService --> AuditLogger : records via
  AuthService --> PrismaClient : queries