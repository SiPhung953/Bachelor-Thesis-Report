%%{init: {
  "theme": "default",
  "htmlLabels": false,
  "themeVariables": {
    "fontFamily": "Times New Roman, serif",
    "fontSize": "18px"
  },
  "class": {
    "diagramPaddingX": 15,
    "diagramPaddingY": 15,
    "nodeSpacing": 40,
    "rankSpacing": 50,
    "hideEmptyMembersBox": true
  }
}}%%

classDiagram
direction LR

  %% ==========================================
  %% 1. PRESENTATION LAYER (Controllers)
  %% ==========================================
  class ResumeController {
    <<Controller>>
    +getMyResume()
    +uploadResume()
    +deleteResume()
  }

  %% ==========================================
  %% 2. APPLICATION LAYER (Services)
  %% ==========================================
  class ResumeService {
    <<Service>>
    +getMyResume()
    +uploadResume()
    +deleteResume()
  }

  %% ==========================================
  %% 3. INFRASTRUCTURE & UTILITY LAYER
  %% ==========================================
  class FileStorageService {
    <<Utility>>
    +saveCv()
    +deleteFile()
  }

  class AssertRole {
    <<Utility>>
    +assertJobSeeker()
  }

  class AuditLogger {
    <<Utility>>
    +log()
  }

  class PrismaClient {
    <<Infrastructure>>
    +user
    +resume
  }

  %% ==========================================
  %% RELATIONSHIPS (Flows Left-To-Right)
  %% ==========================================

  %% Presentation to Application
  ResumeController --> ResumeService : delegates to

  %% Service Dependencies
  ResumeService --> FileStorageService : uses
  ResumeService --> AssertRole : authorizes via
  ResumeService --> AuditLogger : records via
  ResumeService --> PrismaClient : queries