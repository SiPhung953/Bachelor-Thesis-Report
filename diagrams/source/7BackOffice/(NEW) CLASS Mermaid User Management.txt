%%{init: {
  "theme": "default",
  "htmlLabels": false,
  "themeVariables": {
    "fontFamily": "Times New Roman, serif",
    "fontSize": "20px"
  },
  "class": {
    "diagramPaddingX": 15,
    "diagramPaddingY": 15,
    "nodeSpacing": 40,
    "rankSpacing": 50,
    "hideEmptyMembersBox": true
  }
}}%%

classDiagram
direction LR

  class UserManagementController {
    <<Controller>>
    +getUsers()
    +banUser()
    +unbanUser()
  }

  class UserManagementService {
    <<Service>>
    +getUsers()
    +banUser()
    +unbanUser()
  }

  class AssertRole {
    <<Utility>>
    +assertAdmin()
  }

  class AuditLogger {
    <<Utility>>
    +log()
  }

  class PrismaClient {
    <<Utility>>
    +user
    +log
  }

  UserManagementController --> UserManagementService : delegates to
  UserManagementService --> AssertRole : authorizes via
  UserManagementService --> AuditLogger : records via
  UserManagementService --> PrismaClient : queries